{% if content is defined and content %}
	<{{ context.tag }} {{ context | attr('type', 'charset', 'async', 'defer') }}>
	{{ content | raw }}
	</{{ context.tag }}>
{% elseif template is defined and template %}
	{{ include(template, template_data, false) }}
{% endif %}
